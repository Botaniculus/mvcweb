<header>
  <h1>Seznam článků</h1>
</header>

<section>
  <table>
  <?php foreach($articles as $article) : ?>
    <tr>
      <td>
        <h2><a href="clanek/<?= $article['article_url'] ?>"><?= $article['article_title'] ?></a></h2>
        <p class="gray"><?= $article['article_author_name'] ?> | <?= date("j. n. Y", strtotime($article['article_submitted_date'])) ?></p>
        <?= $article['article_description'] ?>
        <?php if ($user_permissions >= 2 || ($article['article_author_id'] == $user_id)) : ?>
          <br />
          <a href="editor/<?= $article['article_url'] ?>">Editovat</a>
          <a href="clanek/<?= $article['article_url'] ?>/odstranit">Odstranit</a>
        <?php endif ?>
      </td>
    </tr>
  <?php endforeach ?>
  </table>
</section>
